# Ollama 本地 AI 安装和配置指南

## 概述

Ollama 是一个开源的本地大语言模型运行工具，可以在你的电脑上运行 Llama、Mistral 等开源模型，完全离线使用，保护隐私。

Silent Feed 支持使用 Ollama 作为本地 AI 提供商，用于内容推荐和分析。

## 为什么选择 Ollama？

✅ **完全本地运行** - 数据不离开你的设备，隐私保护  
✅ **免费使用** - 无需 API 费用  
✅ **离线可用** - 无需网络连接  
✅ **多模型支持** - Llama 3.2, Mistral, Gemma 等  
✅ **简单易用** - 命令行操作，几分钟完成安装

## 系统要求

- **macOS**: 11 Big Sur 或更高版本
- **Linux**: Ubuntu 18.04 或更高版本，其他发行版也支持
- **Windows**: WSL2 或 Windows 11
- **内存**: 至少 8GB RAM（推荐 16GB）
- **磁盘**: 每个模型约 4-8GB 空间

## 安装步骤

### macOS

使用 Homebrew 安装（推荐）：

```bash
# 1. 安装 Ollama
brew install ollama

# 2. 启动 Ollama 服务
ollama serve
```

或直接下载安装包：

1. 访问 [Ollama 官网](https://ollama.com/download)
2. 下载 macOS 安装包
3. 双击安装
4. 启动 Ollama 应用

### Linux

```bash
# 一键安装脚本
curl -fsSL https://ollama.com/install.sh | sh

# 启动服务
ollama serve
```

### Windows

1. 安装 WSL2（如果还未安装）
2. 在 WSL2 中按 Linux 步骤安装
3. 或访问 [Ollama 官网](https://ollama.com/download) 下载 Windows 版本

## 下载和使用模型

### 推荐模型

根据你的硬件配置选择合适的模型：

#### 1. Llama 3.2 (推荐用于 Silent Feed)

```bash
# 3B 版本 - 轻量级，适合日常使用（约 2GB）
ollama pull llama3.2

# 1B 版本 - 超轻量，最快速度（约 1GB）
ollama pull llama3.2:1b
```

**优点**: Meta 官方模型，性能优秀，速度快

#### 2. Qwen 2.5 (中文优化)

```bash
# 7B 版本 - 中文理解能力强（约 4.5GB）
ollama pull qwen2.5:7b

# 3B 版本 - 轻量级中文模型（约 2GB）
ollama pull qwen2.5:3b
```

**优点**: 阿里巴巴开源，对中文内容理解更好

#### 3. Gemma 2 (Google 出品)

```bash
# 2B 版本 - 轻量高效（约 1.5GB）
ollama pull gemma2:2b
```

**优点**: Google 官方，质量可靠

### 模型选择建议

| 硬件配置 | 推荐模型 | 说明 |
|---------|---------|------|
| 8GB RAM | llama3.2:1b 或 gemma2:2b | 轻量快速 |
| 16GB RAM | llama3.2 或 qwen2.5:3b | 平衡性能 |
| 32GB+ RAM | qwen2.5:7b | 最佳效果 |

### 测试模型

下载模型后，可以在命令行测试：

```bash
# 交互式对话
ollama run llama3.2

# 单次问答
ollama run llama3.2 "你好，介绍一下自己"

# 退出对话
/bye
```

## 在 Silent Feed 中配置

### 1. 确保 Ollama 服务运行

```bash
# 检查服务状态
curl http://localhost:11434/api/version

# 如果未运行，启动服务
ollama serve
```

### 2. 在扩展设置中配置

1. 打开 Silent Feed 设置页面
2. 找到 **AI 提供商** 设置
3. 选择 **Ollama**
4. 配置以下选项：
   - **API 地址**: `http://localhost:11434`（默认）
   - **模型名称**: `llama3.2`（或你下载的其他模型）
   - **启用状态**: 开启

### 3. 测试连接

点击设置页面的 **测试连接** 按钮，确认：
- ✅ Ollama 服务可访问
- ✅ 模型已下载
- ✅ 能正常生成内容

## 常见问题

### Q: Ollama 服务如何后台运行？

**macOS/Linux**:
```bash
# 使用 nohup 后台运行
nohup ollama serve > /dev/null 2>&1 &

# 或创建系统服务（macOS）
brew services start ollama
```

### Q: 模型下载太慢怎么办？

可以使用镜像或手动下载：
```bash
# 使用环境变量指定镜像
export OLLAMA_HOST=https://registry.cn-hangzhou.aliyuncs.com
ollama pull llama3.2
```

### Q: 如何查看已下载的模型？

```bash
ollama list
```

### Q: 如何删除不需要的模型？

```bash
ollama rm llama3.2
```

### Q: 内存不足怎么办？

1. 使用更小的模型（1B/2B 版本）
2. 限制并发请求数量
3. 在设置中调低模型参数（temperature, context length）

### Q: 如何更新 Ollama？

**macOS**:
```bash
brew upgrade ollama
```

**Linux**:
```bash
curl -fsSL https://ollama.com/install.sh | sh
```

## 性能优化建议

### 1. 调整模型参数

在 Silent Feed 设置中可以调整：
- **Temperature**: 降低到 0.3-0.7 可以更稳定
- **Max Tokens**: 限制在 500-1000 可以加快速度
- **Context Length**: 减小可以节省内存

### 2. 使用量化模型

Ollama 默认使用量化模型以平衡性能和质量：
```bash
# 查看可用的量化版本
ollama show llama3.2

# 使用特定量化版本
ollama pull llama3.2:q4_0  # 4-bit 量化
```

### 3. GPU 加速

如果有 NVIDIA GPU（Linux）：
```bash
# Ollama 会自动检测并使用 GPU
# 确保安装了 NVIDIA 驱动和 CUDA
nvidia-smi  # 检查 GPU 状态
```

## 进阶使用

### API 调用示例

Ollama 提供标准的 HTTP API：

```bash
# 生成文本
curl http://localhost:11434/api/generate -d '{
  "model": "llama3.2",
  "prompt": "介绍一下北京",
  "stream": false
}'

# 对话模式
curl http://localhost:11434/api/chat -d '{
  "model": "llama3.2",
  "messages": [
    {"role": "user", "content": "你好"}
  ]
}'
```

### Modelfile 自定义

创建自定义模型配置：

```bash
# 创建 Modelfile
cat > Modelfile << EOF
FROM llama3.2
SYSTEM 你是 Silent Feed 的 AI 助手，专门帮助用户分析和推荐网页内容。请用简洁的中文回答。
PARAMETER temperature 0.5
PARAMETER top_p 0.9
EOF

# 构建自定义模型
ollama create silentfeed-assistant -f Modelfile

# 使用自定义模型
ollama run silentfeed-assistant
```

## 资源链接

- 📘 [Ollama 官方文档](https://github.com/ollama/ollama/blob/main/docs/README.md)
- 🤗 [模型库](https://ollama.com/library)
- 💬 [Discord 社区](https://discord.gg/ollama)
- 🐛 [问题反馈](https://github.com/ollama/ollama/issues)

## 未来支持计划

### Chrome AI (暂未支持)

Chrome 内置的 Gemini Nano 模型目前仍在实验阶段，我们计划在以下条件满足后添加支持：

- ✅ Chrome 138+ 稳定版广泛普及
- ✅ Prompt API 正式发布
- ✅ macOS/Linux 全平台可用
- ✅ 模型下载和配置更简便

届时 Chrome AI 将成为另一个隐私友好的本地 AI 选项。

---

**最后更新**: 2025-01-28  
**维护者**: Silent Feed 团队
