# 发布前清理工作

**日期**: 2025-11-19  
**分支**: chore/pre-release-cleanup  
**目标**: 准备v1.0发布

---

## 清理任务清单

### ✅ 已完成

1. **删除临时备份文件**
   - 删除 `src/components/settings/ProfileView.tsx.backup`

2. **清理调试日志**
   - 移除 `src/popup.tsx` 中的 3 个 console.log
   - 移除 `src/components/settings/AIConfig.tsx` 中的 console.error
   - 保留 `src/debug/` 和 `src/i18n/` 中的日志（开发工具需要）

3. **代码提交**
   - 提交: `chore: 清理临时文件和调试日志`
   - 文件: 3 个修改，700 行删除

---

### 🔄 进行中

#### 1. 修复测试失败（8/12 失败）

**问题分析**:
- 测试文件使用的是旧的翻译键 `options.recommendation.*`
- 组件已更新为新的翻译键 `options.analysisEngine.*`
- 需要更新测试 mock 以匹配新的翻译结构

**受影响文件**:
- `src/components/settings/RecommendationSettings.test.tsx`

**需要更新的翻译键映射**:
```typescript
// 旧键 → 新键
"options.recommendation.title" → "options.analysisEngine.recommendationTitle"
"options.recommendation.currentMode" → 需要查找实际使用的键
"options.recommendation.reasoningAI" → 需要查找实际使用的键
// ... 其他键
```

**下一步**:
1. 提取组件中所有使用的翻译键
2. 从实际翻译文件中获取对应的英文翻译
3. 更新测试 mock
4. 运行测试确保全部通过

---

### ⏳ 待开始

#### 2. 处理 TODO 注释

**发现的 TODO**:
