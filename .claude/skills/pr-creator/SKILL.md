---
name: pr-creator
description: Intelligent PR creation assistant for Silent Feed project. Use this skill when the user wants to create a pull request, needs help with PR description, or mentions "创建PR" / "create PR". Analyzes commits and code changes to generate comprehensive, well-structured PR descriptions following project conventions.
---

# PR Creator Skill

智能 PR 创建助手，专为 Silent Feed 项目优化。

## 触发条件

当用户提到以下任一情况时使用此 skill：
- "创建 PR" / "create PR"
- "生成 PR 描述"
- "帮我写 PR 说明"
- "分析这些提交"
- 准备合并代码到 master

## 核心功能

### 1. 提交分析

```bash
# 获取所有待合并的提交
git log origin/master..HEAD --oneline

# 获取详细提交信息
git log origin/master..HEAD --pretty=format:"%h %s%n%b"

# 查看变更文件
git diff --name-status origin/master..HEAD

# 查看代码差异
git diff origin/master..HEAD --stat
```

### 2. 智能分类

根据提交和文件变更自动识别：

**变更类型**：
- `feat:` / `feature/` → 新功能
- `fix:` / `bugfix` → Bug 修复
- `refactor:` → 重构
- `perf:` → 性能优化
- `docs:` → 文档更新
- `test:` → 测试相关
- `chore:` → 构建/工具链

**影响范围**：
- `src/core/` → 核心功能
- `src/components/` → UI 组件
- `src/storage/` → 数据存储
- `src/background.ts` → 后台服务
- `*.test.ts` → 测试覆盖

**版本建议**（基于 Semantic Versioning）：
- 包含 `!` 或 `BREAKING` → major
- 包含 `feat:` → minor
- 包含 `fix:` / `refactor:` → patch

### 3. PR 描述结构

生成符合项目规范的 PR 描述，包含：

```markdown
## 🤫 概述
[简洁的一句话总结，30字以内]

## 📝 变更内容

### 主要功能
- [核心变更点1]
- [核心变更点2]

### 次要改进
- [小改进1]
- [小改进2]

## 🎯 解决的问题

[描述这个 PR 解决了什么问题或满足了什么需求]

## 💡 技术实现

### 架构变更
[如有重要的架构调整，说明设计思路]

### 关键代码
```typescript
// 核心逻辑示例
```

### 依赖变更
- 新增依赖：xxx
- 升级依赖：xxx

## 🧪 测试

- [x] 所有单元测试通过 (X/Y passed)
- [x] 覆盖率达标 (X%)
- [x] 手动测试场景：
  - 场景1
  - 场景2

## 📊 影响范围

**修改的文件**：X files changed, +XXX -YYY
**核心模块**：
- `src/xxx` - [说明]
- `src/yyy` - [说明]

**用户可见变化**：
- [UI/功能变化描述]

**向后兼容性**：
- ✅ 完全向后兼容
- ⚠️ 需要数据迁移
- ❌ 破坏性变更

## 🔄 数据迁移

[如有数据库或存储结构变更，说明迁移方案]

## 📸 截图/演示

[如有 UI 变更，提供截图或 GIF]

## ✅ 检查清单

- [ ] 代码已自测
- [ ] 测试用例已添加/更新
- [ ] 文档已更新
- [ ] CHANGELOG 已更新（如需要）
- [ ] 版本号已更新

## 🔗 相关 Issue/PR

- Closes #XXX
- Related to #YYY

---
*Generated by 🤫 Silent Feed PR Creator*
```

### 4. 特殊考虑

**Silent Feed 项目特性**：
- 使用 🤫 emoji 作为品牌标识
- 推荐系统架构：原料池 → AI分析 → 候选池 → 推荐池
- 数据库使用 Dexie（IndexedDB）
- 测试框架 Vitest，覆盖率要求 ≥70%
- i18n 使用 react-i18next，所有用户可见文本需 `_()` 包裹
- Chrome MV3 扩展架构

**版本号规则**：
- 当前版本从 `package.json` 读取
- 需要检查是否已更新版本号
- 建议更新方式：`npm run version:patch/minor/major`

## 工作流程

1. **分析变更**
   ```bash
   # 运行命令获取提交和差异
   git log origin/master..HEAD --oneline
   git diff --stat origin/master..HEAD
   ```

2. **理解语义**
   - 阅读提交信息理解变更意图
   - 查看关键文件的 diff（如需要）
   - 识别测试覆盖情况

3. **生成描述**
   - 按模板结构组织内容
   - 使用项目约定的 emoji 和术语
   - 确保描述清晰、准确、完整

4. **创建文件**
   ```bash
   # 将 PR 描述保存到临时文件
   cat > /tmp/pr-description.md << 'EOF'
   [生成的 PR 描述]
   EOF
   ```

5. **执行创建**
   ```bash
   # 使用文件创建 PR
   gh pr create \
     --title "type: 简短标题" \
     --body-file /tmp/pr-description.md \
     --label "label1,label2"
   ```

## 示例对话

**用户**：帮我创建一个 PR，包含最近的提交

**助手行动**：
1. 运行 `git log origin/master..HEAD` 查看提交
2. 运行 `git diff --stat origin/master..HEAD` 查看变更
3. 分析提交信息，识别变更类型
4. 生成结构化 PR 描述
5. 保存到 `/tmp/pr-description.md`
6. 询问标题和标签
7. 执行 `gh pr create --body-file /tmp/pr-description.md`

## 提示词模板

当用户请求创建 PR 时，使用此模板：

```
让我分析一下当前分支的变更：

[运行 git 命令获取提交和差异]

根据分析，这个 PR 的主要变更是：
- [变更点1]
- [变更点2]

建议版本类型：[patch/minor/major]
建议标签：[label1, label2]

我将生成详细的 PR 描述...

[生成并显示 PR 描述]

PR 描述已保存到 /tmp/pr-description.md

建议的 PR 标题：
"type: 简短描述"

是否使用此标题创建 PR？或者你想修改标题？
```

## 注意事项

1. **敏感信息**：检查 PR 描述中不要包含敏感信息（API keys、密码等）
2. **测试状态**：确认测试已通过再创建 PR
3. **版本号**：提醒用户检查版本号是否已更新
4. **图片资源**：如有 UI 变更，建议用户提供截图
5. **向后兼容**：明确标注是否有破坏性变更

## 快捷命令参考

```bash
# 查看提交
git log origin/master..HEAD --oneline

# 查看详细变更
git log origin/master..HEAD --stat

# 查看文件差异
git diff origin/master..HEAD

# 创建 PR
gh pr create --title "xxx" --body-file /tmp/pr-description.md

# 添加标签
gh pr edit --add-label "enhancement,feature"
```
